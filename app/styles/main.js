import{
    StyleSheet
} from 'react-native';

const mainStyle = StyleSheet.create({
    p: {
       color: '#5bc0de',
       fontSize: 30,
       textAlign: 'center',
       marginBottom: 20
    },
    container: {
        justifyContent: 'space-evenly',
        alignItems: 'center',
        flex: 1,
    },
});

export default mainStyle;